GEMINI_API_KEY=your_gemini_api_key_here
SECRET_TOKEN=your_random_secret_here
ALLOWED_ORIGIN=https://claude.ai
PORT=3000

# For free-tier image generation via /api/generate-image,
# only GEMINI_API_KEY is required. SECRET_TOKEN and ALLOWED_ORIGIN
# are optional — the free-tier endpoint has no auth or CORS checks.
